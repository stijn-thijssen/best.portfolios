# Open Graph image

`og-image.jpg` is generated at build time by `scripts/generate-seo.mjs` (dark brand placeholder, 1200×630).

Replace it with a designed asset (same path and dimensions) for production social previews. After updating, run `pnpm generate:seo` or `pnpm build`.
